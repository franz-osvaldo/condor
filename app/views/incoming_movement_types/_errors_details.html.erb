<div id="modal" class="modal fade modal_delete" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <span class="glyphicon glyphicon-bullhorn" aria-hidden="true"></span> Validación
      </div>
      <div class="modal-body text-center">
        <br>
        <p style="color:#da4f4a;"><span class="glyphicon glyphicon-fire" aria-hidden="true"></span></p>
            <p><strong><%= incoming_movement_type.movement_type %></strong></p>
            <% incoming_movement_type.errors.full_messages.each do |message| %>
                <p><%= message  %></p>
            <% end %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default btn-sm" data-dismiss="modal">Cerrar</button>
      </div>
    </div>
  </div>
</div>