$('.modal_container').html('<%= escape_javascript(render partial: 'full_details', locals: {incoming_movement: @incoming_movement}) %>');

$("#modal").on('shown.bs.modal', function () {
  $(".nano.body_container").nanoScroller({ alwaysVisible: true });
});
$('#modal').modal('show');
$(".nano").nanoScroller({ scroll: 'top' });
