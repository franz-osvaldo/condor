<div class="row" id="<%= dom_id(outgoing_tool_type) %>">
  <div class="col-md-6 item_crud overflowed"><%= outgoing_tool_type.movement_type %></div>
  <div class="col-md-2">
    <%= link_to outgoing_tool_type, class: 'btn btn-info btn-sm', remote: true do  %>
        <span class="glyphicon glyphicon-search"></span> Mostrar
    <% end %>
  </div>
  <div class="col-md-2">
    <%= link_to edit_outgoing_tool_type_path(outgoing_tool_type), class: 'btn btn-warning btn-sm', remote: true  do %>
        <span class="glyphicon glyphicon-pencil"></span> Editar
    <% end %>
  </div>
  <div class="col-md-2">
    <button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target = "#modal-<%= dom_id(outgoing_tool_type) %>">
      <span class="glyphicon glyphicon-trash"></span> Eliminar
    </button>
  </div>
</div>

<div id="modal-<%= dom_id(outgoing_tool_type) %>" class="modal fade modal_delete" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Eliminar
      </div>
      <div class="modal-body">
        <p>¿Estas seguro?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default btn-sm" data-dismiss="modal">Cancelar</button>
        <%= link_to outgoing_tool_type, class: 'btn btn-danger btn-sm', method: :delete, remote: true do %>
            Eliminar
        <% end %>
      </div>
    </div>
  </div>
</div>